import React, { Component } from 'react' 

class Boton extends Component {
    render () {
        return (
            <button onClick={() => alert("clickea2")}>CLICKEAME</button>
        )
    }
}

export default Boton